export const API_CONFIG = Object.freeze({
  FETCH_DELAY_MIN: 1000,
  FETCH_DELAY_MAX: 1500,
  FETCH_CUSTOMERS_FAILURE_RATE: 0.2,
  FETCH_CUSTOMER_FAILURE_RATE: 0.05,
  ERROR_MESSAGES: Object.freeze({
    FETCH_TRANSACTIONS: "Failed to fetch transactions.",
  }),
});
